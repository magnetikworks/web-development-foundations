# MCQ 3

1. Which of the following best describes 'Asynchronous' execution in JavaScript? 
    1. A way to force all functions to run at exactly the same millisecond.
    2. Code that runs line-by-line and stops the browser until each task is finished.
    3. A method used to encrypt data before sending it to a server.
    4. Code that allows the browser to continue handling other tasks while waiting for a long-running process to complete.

2. What are the three possible states of a JavaScript Promise?
    1. Input, Process, Output
    2. Start, Middle, End
    3. Local, Session, Global
    4. Pending, Resolved, Rejected
  
3. When using the fetch() API, what is the purpose of the first .then() block (or the first await)?
    1. To physically move the data into a HTML <div>.
    2. To convert the raw HTTP response into a usable JSON object.
    3. To tell the browser to stop all other scripts until the data arrives.
    4. To delete the data from the server after it has been read.

4. Which JavaScript method is used to prevent a Contact Form from refreshing the entire page when the user clicks "Submit"?
    1. e.stopPropagation()
    2. window.stop()
    3. e.preventDefault()
    4. form.reset()

5. Why is it considered a "Best Practice" to use a try...catch block when fetching data from an external API?
    1. It makes the code run faster by skipping unnecessary steps.
    2. It allows the program to handle errors (like a 404 or no internet) without crashing the entire site.
    3. It is a security requirement to prevent hackers from seeing the API URL.
    4. It automatically translates the API data into different languages.
