# MCQ 3

1. Which of the following best describes 'Asynchronous' execution in JavaScript? 
    1. A way to force all functions to run at exactly the same millisecond.
    2. Code that runs line-by-line and stops the browser until each task is finished.
    3. A method used to encrypt data before sending it to a server.
    4. Code that allows the browser to continue handling other tasks while waiting for a long-running process to complete.

2. What are the three possible states of a JavaScript Promise?
    1. Input, Process, Output
    2. Start, Middle, End
    3. Local, Session, Global
    4. Pending, Resolved, Rejected
