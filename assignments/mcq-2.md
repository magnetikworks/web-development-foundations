# MCQ #2

1. Question: Which keyword should be used by default when declaring a variable that you do not expect to reassign later in the code?

Options:
A. var
B. let
C. static
D. const

2. Question: What will be the result of the following operation in the console?

```js
console.log(10 + "5");
```
Options:
A. NaN
B. 15
C. "105"
D. Error

3. Question: Why is it recommended to use the strict equality operator (===) instead of the loose equality operator (==) in JavaScript?

Options:
A. It allows variables to be compared across different functions.
B. It automatically converts strings to numbers.
C. It is faster for the browser to execute.
D. It checks for both value and data type.

4. Question: In the context of function scope, what happens to a variable declared with let inside a function?

Options:
A. It can only be accessed within that specific function.
B. It is moved to the top of the script automatically.
C. It can be accessed by other functions in the same file.
D. It becomes a global variable accessible anywhere.

5. Question: What is the primary purpose of the return keyword within a function?

Options:
A. To print a message to the browser console.
B. To link a JavaScript file to an HTML document.
C. To stop the function from ever running.
D. To send a value back to the caller so it can be used elsewhere.
